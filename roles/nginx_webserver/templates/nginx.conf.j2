user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 768;
}

http {
    server {
        listen {{ port_no }};
        server_name {{ server_name }};

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
